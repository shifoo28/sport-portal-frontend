import { List, Menu } from "@material-tailwind/react";
import React from "react";
import MenuItem from "./MenuItem";

const MenuC = async () => {

  return (
    <Menu>
      <List>
        {/* {menuItems.map((menu:any, index:any) => {
          const depthLevel = 0;

          return <MenuItem></MenuItem>;
        })} */}
      </List>
    </Menu>
  );
};

export default MenuC;
